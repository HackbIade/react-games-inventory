import { IconButton } from "@mui/material";
import styled from "styled-components";

export const IconButtonWrapper = styled(IconButton).attrs({
  "aria-label": "info",
})`
  right: 16px;
  bottom: 16px;
  position: fixed;
`;
